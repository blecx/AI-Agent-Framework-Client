# AI Agent Framework Client - Environment Configuration

# ==============================================================================
# API Configuration
# ==============================================================================

# Base URL for the AI Agent Framework API
# This is injected at BUILD TIME (not runtime), so rebuild after changes
VITE_API_BASE_URL=http://localhost:8000/api

# Optional: API key for authentication (if enabled on API)
VITE_API_KEY=

# ==============================================================================
# Health Check Configuration
# ==============================================================================

# Interval for health check polling (milliseconds)
# Default: 30000 (30 seconds)
VITE_HEALTH_CHECK_INTERVAL=30000

# ==============================================================================
# Environment-Specific Examples
# ==============================================================================

# Development (local API)
# VITE_API_BASE_URL=http://localhost:8000/api

# Production (Docker internal networking)
# Use this when both client and API are in Docker on same network
# VITE_API_BASE_URL=http://ai-agent-api:8000/api

# Production (External/Public API)
# Use this for production deployments with public API
# VITE_API_BASE_URL=https://api.yourdomain.com/api

# ==============================================================================
# Important Notes
# ==============================================================================

# 1. All environment variables MUST be prefixed with VITE_ to be accessible
# 2. Variables are injected at BUILD TIME, not runtime
# 3. After changing this file, you must rebuild:
#    - Development: Restart dev server (npm run dev)
#    - Production: Rebuild (npm run build or docker compose build)
# 4. Never commit .env files with secrets to version control
# 5. Copy this file to .env and customize for your environment:
#    cp .env.example .env
