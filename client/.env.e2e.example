# E2E Testing Environment Variables
# Copy this to .env.e2e and customize for your local environment

# Frontend URL - where the client app is served
E2E_BASE_URL=http://localhost:3000

# Backend API URL - where the AI-Agent-Framework API is running
API_BASE_URL=http://localhost:8000

# Optional: Run in headless mode (default: true in CI, false locally)
# E2E_HEADLESS=false

# Optional: Backend directory (for setup script)
# BACKEND_DIR=../../AI-Agent-Framework
